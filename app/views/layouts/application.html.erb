<!DOCTYPE html>
<html>
<head>
  <title>MovieHub - Your Movie Collection</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="icon" href="data:,">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

  <style>
    /* ===== CINEMATIC GLOBAL THEME ===== */
    body {
      background-color: #0b0b0d;
      color: #f8f8f8;
      font-family: 'Poppins', 'Segoe UI', sans-serif;
    }

    /* ===== NAVBAR ===== */
    .navbar {
      background: linear-gradient(90deg, #150000, #2b0000, #000);
      border-bottom: 1px solid #ff1a75;
      box-shadow: 0 0 15px rgba(255, 0, 0, 0.4);
    }

    .navbar-brand {
      color: #ff4d4d !important;
      font-weight: 700;
      text-shadow: 0 0 10px #ff1a75;
      font-size: 1.6rem;
      margin: 0 auto;
      display: block;
      text-align: center;
    }

    /* ===== RED BUTTONS FIX ===== */
    .btn-primary {
      background: linear-gradient(90deg, #ff4d4d, #ff1a75) !important;
      border: none !important;
      color: #fff !important;
      box-shadow: 0 0 10px #ff1a75 !important;
      font-weight: 500;
    }
    .btn-primary:hover {
      background: linear-gradient(90deg, #ff1a75, #ff4d4d) !important;
      transform: scale(1.03);
    }

    /* ===== SECONDARY BUTTON ===== */
    .btn-secondary {
      background-color: #333 !important;
      color: #ccc !important;
      border: none !important;
    }
    .btn-secondary:hover {
      background-color: #444 !important;
      color: #fff !important;
    }

    /* ===== ALERTS ===== */
    .alert {
      border-radius: 12px;
      background-color: #1a1a1a;
      color: #ff4d4d;
      border: 1px solid #ff1a75;
      box-shadow: 0 0 10px rgba(255, 0, 0, 0.3);
    }
    .btn-close {
      filter: invert(1);
    }

    /* ===== CONTAINER ===== */
    .container {
      padding-top: 25px;
      max-width: 1000px;
    }

    /* ===== FOOTER ===== */
    footer {
      background: linear-gradient(90deg, #1a0000, #330000);
      border-top: 1px solid #ff1a75;
      color: #ff9999;
      text-shadow: 0 0 4px #660000;
    }

    footer p {
      margin-bottom: 0;
      font-size: 0.95rem;
    }

    footer a {
      color: #ff4d4d;
      text-decoration: none;
      font-weight: 500;
    }

    footer a:hover {
      color: #fff;
      text-shadow: 0 0 10px #ff1a75;
    }
  </style>
</head>
<body>
  <!-- ===== NAVBAR (Centered Logo, No Links) ===== -->
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand" href="/">
        <i class="bi bi-film"></i> MovieHub
      </a>
    </div>
  </nav>

  <!-- ===== PAGE CONTENT ===== -->
  <div class="container mt-4">
    <% if notice %>
      <div class="alert alert-dismissible fade show" role="alert">
        <%= notice %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    <% end %>

    <%= yield %>
  </div>

  <!-- ===== FOOTER ===== -->
  <footer class="mt-5 py-4 text-center">
    <div class="container">
      <p>
        &copy; 2025 <strong>MovieHub</strong> — <em>Your Personal Movie Collection</em><br>
        <a href="#">Back to Top</a>
      </p>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
